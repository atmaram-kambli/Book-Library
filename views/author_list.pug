extends layout

block content
  h1= title

  if author_list.length
    ul
      each author in author_list
        li
          a(href=author.url) #{author.fullName}
          |  (#{author.dataOfBirth} - #{author.dateOfDeath})
  else
    p There are no authors.
